# 🗳️ BitBallot: Atomic Display Integrity for Public Elections

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![PRs Welcome](https://img.shields.io/badge/PRs-welcome-brightgreen.svg?style=flat-square)](http://makeapullrequest.com)
[![Rust](https://img.shields.io/badge/Rust-1.70+-orange.svg)](https://www.rust-lang.org/)

vProgsとSTARKを活用し、国家規模の選挙に「表示の真正性」と「スケーラビリティ」をもたらす次世代投票プロトコル

---

## 🌟 プロジェクト概要

BitBallotは、**Kaspa**などのBlockDAGアーキテクチャをL1（データ可用性・順序付け層）として活用する投票システムです。 
既存の電子投票が抱える「端末改ざんによる意図のすり替え」や「集計中の中間情報漏洩」を暗号技術で解決しつつ、監視された投票所という現実的な制度的運用を前提としています。

## 🚀 クイックスタート (Quick Start)

プロジェクトの全体像をローカルで確認するための手順です。

```bash
# クライアント(WASM)のビルド準備
cd client-wasm
cargo build --target wasm32-unknown-unknown

# Webアプリの起動
cd ../web-app
npm install
npm run dev
```

## ✨ 主要機能 (Core Features)

### 🔒 Atomic Display Integrity (ADI)
有権者の意図、画面表示、および記録される暗号文を**単一のアトミックな承認イベント**として結合します。  
これにより、端末が改ざんされていても「画面表示と異なる候補者」を裏で承認させる攻撃を不可能にします。

### 🎭 集約されたダミー投票 (Aggregated Batching)
1つの真の投票と複数のダミー投票をローカルで一括処理します。  
これらは単一のSTARK証明として集約され、L1には1つのトランザクションとして提出されるため、外部から真の意図を特定することはできません。
※本実装では「本命1枚 + ダミー9枚 = 合計10枚」のバッチ構成を採用しています。

### 🏁 ターミナル・コンプリート（終端完了型）集計
投票期間中、L1はデータの順序を確定させるだけで、**途中集計は一切行いません**。  
選挙終了後に初めて、ガーディアン（後見人）が閾値復号とゼロ知識証明を用いて最終結果を算出します。

### 🛡️ 最終投票有効（Last-Vote-Valid）セマンティクス
一人の有権者が複数回投票（再投票）することを許容し、常に**「最後に提出された有効な投票」**を最終結果として採用します。  
これにより、買収や脅迫による一時的な投票指示を無効化する余地を与えます。

### 🌐 検閲耐性オートルーティング (Anti-Censorship Routing)
特定のノードによる意図的なトランザクションの破棄（投票の検閲）を防ぐため、候補者・政党ごとに独立した入口ノードを構築できる設計になっています。
システムは有権者が選択した候補者の陣営ノードへ投票データを自動で優先ルーティングし、万が一妨害やサーバーダウンがあった場合は中立ノードへ即座にフォールバックします。この仕組みと「ダミー投票の集約」を組み合わせることで、ノード運用者ですら真の意図を判別できなくなり、あらゆる検閲を無力化します。

### ⚡ メガバッチ集約による超低コスト運用 (Zero-cost Rollup)
各投票所端末から送られる個別の投票トランザクションは、受信した候補者ノード側で数千〜数万件規模の「メガバッチ（再帰的STARK証明）」へと集約（Rollup）されてからL1に提出されます。
これにより、有権者1人あたりのL1トランザクション手数料（書き込みスペース）が事実上ゼロにまで極小化され、数千万から億規模の国家選挙であっても極めて低コストで現実的なインフラ運用を可能にします。

### 💸 既存システムとの運用コスト比較 (1億人規模想定)
米国大統領選挙クラス（投票者約1億人）を想定した場合、BitBallotのSTARKとRollup（メガバッチ集約）アーキテクチャは、選挙ITインフラにかかるコスト構造を根本から破壊するパラダイムシフトをもたらします。

| インフラ維持・IT処理項目 | 既存の電子/紙投票システム | BitBallot (1万件/メガバッチ集約) |
| :--- | :--- | :--- |
| **データ登録・集計基盤** | 莫大な専用サーバー保守／専用線の維持費 | **実質無料** (L1を不変の掲示板として相乗り利用) |
| **ネットワークの書き込み手数料** | - | **約1万〜数万円** (10,000回のL1へのTx送信) |
| **セキュリティ検証・暗号計算** | 監査および立会人に関わる莫大な人件費 | **約数千万円** (証明生成用クラウド計算費：1票1円以下) |
| **1票あたりのIT処理原価** | 通常 数千円〜 | **1円以下 (究極のコスト破壊)** |

> [!NOTE]
> 投票所の物理的な設営費用は残りますが、データ保護・集計・改ざん防止に関わるITシステムの運用コストが、国家レベルであっても「数千万円（マクドナルド数店舗の維持費）」のクラウド計算リソースのみにまで圧縮されます。STARKの数学的安全性は、計算コストが0に近づいても一切劣化しません。

## 🏗️ 技術アーキテクチャと将来展望

### 1. 実行に依存しないL1の活用
BitBallotはスマートコントラクトや専用のL2チェーンを必要としません。
- **L1 (Kaspa等)**: 不変のデータ保存と順序付け（Ordering）のみを担当。
- **オフチェーン実行**: 投票ロジックの検証は、各ノードがオフチェーンで行う「検証可能なプログラム（vProg）」形式で実施。

### 2. 候補者ノードでの証明集約と無限のスケーラビリティ
投票端末は個別のバッチの正当性を証明する軽量なSTARKを生成し、直ちにデータを破棄する「ステートレス」な運用を行います。
- **バッチの集約 (Aggregated Rollup)**: 端末から一斉送信された大量の暗号化Txは、24時間運用される堅牢な候補者ノード群によって受け止められ、数千～数万件分の一つの巨大な「メガバッチ（再帰的STARK証明）」に集約されてL1に提出されます。
- **究極のデータ削減と検閲の無意味化**: ノードが集約を担うことで、L1のブロックスペース消費を極小化し無限のスケーラビリティを実現します。また、ノード側は集約する際に「どのTxが自陣への本命か」を判別できないため、集約プロセスにおける特定データの意図的な破棄（検閲）は自爆リスクとなり発生しません。
- **並列検証**: L1ノードは投票時に重い証明の検証を行う必要がなく、集計フェーズで一括検証が可能です。

### 3. 汎用ハードウェアの採用 (Cost Effectiveness)
高価な専用ハードウェアや信頼実行環境（TEE）を排除し、オフィス用PCなどの安価な汎用ハードウェアで動作するよう設計されています。

## ⚠️ 重要な設計上の前提条件

- **監視下での運用 (Supervised Environment)**: 自宅などでのリモート投票は、物理的な強要や監視を防げないため、明示的に「対象外」とされています。
- **制度的責任の分離**: 暗号技術は「正当性」を保証しますが、「有権者の資格確認」や「強要の防止」は、既存の法的・物理的な制度（投票所や住民登録）が担います。

## 🤝 貢献について (Contributing)

BitBallotはオープンソースプロジェクトとして、有志による貢献を歓迎しています。詳細は [CONTRIBUTING.md](./CONTRIBUTING.md) を参照してください。

## 📄 ライセンス (License)

本プロジェクトは **MIT License** の下で公開されています。詳細は [LICENSE](./LICENSE) ファイルを参照してください。

---
*This implementation is a Rust/WebAssembly prototype demonstrating the core concepts of BitBallot.*
